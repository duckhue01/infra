---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - script.yaml   # code path  — Python script in a ConfigMap
  - config.yaml   # config path — CronOperation, RBAC, and target resources
