apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: cert-monitor-target
  namespace: operations
spec:
  rules:
  - host: example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: example-service
            port:
              number: 80
